import{readFile,stat}from'node:fs/promises';const manifest=JSON.parse(await readFile('public/manifest.webmanifest','utf8'));if(manifest.start_url!=='/snackquest/app'||manifest.scope!=='/snackquest/')throw new Error('manifest subpath mismatch');if(!(await stat('public/assets/js/scanner.js')).size)throw new Error('scanner bundle missing');const sw=await readFile('public/sw.js','utf8');if(!sw.includes("privatePath")||!sw.includes("SCOPE='/snackquest'"))throw new Error('private cache guard missing');const app=await readFile('public/assets/js/app.js','utf8');if(!app.includes("navigator.serviceWorker.register('/snackquest/sw.js'")||app.includes("addEventListener('load',()=>navigator.serviceWorker.register"))throw new Error('service worker registration is delayed or missing');console.log('PASS manifest, scanner bundle, early service worker registration and private-cache guard');
