import{readFile,stat}from'node:fs/promises';const manifest=JSON.parse(await readFile('public/manifest.webmanifest','utf8'));if(manifest.start_url!=='/snackquest/app'||manifest.scope!=='/snackquest/')throw new Error('manifest subpath mismatch');if(!(await stat('public/assets/js/scanner.js')).size)throw new Error('scanner bundle missing');const sw=await readFile('public/sw.js','utf8');if(!sw.includes("privatePath")||!sw.includes("SCOPE='/snackquest'"))throw new Error('private cache guard missing');console.log('PASS manifest, scanner bundle and private-cache guard');
